{
  "version": 1,
  "prefixes": [
    {
      "prefix": "di-",
      "scope": "department",
      "owner": "Data Infra",
      "description": "DI department-wide skills, agents, and rules."
    }
  ],
  "policy": {
    "enforce": "error",
    "rationale": "Strict mode — skill names without a declared prefix fail validation. Add a new prefix entry above (and an ADR in docs/decisions/) before introducing a skill outside the declared taxonomy."
  }
}
