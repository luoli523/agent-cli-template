{
  "_readme": "This is a credentials template. Copy it to ~/.config/di/credentials.json and fill in real values. Never commit real credentials.",
  "_credential_path": "~/.config/di/credentials.json",

  "_example_service": {
    "_comment": "Each skill that needs credentials declares an entry here. Key format: <service>.<field>. When the future 'di' installer merges this template, it prompts the user to fill in placeholder values.",
    "token": "<fill-me>",
    "base_url": "<https://your-service.example.com/api/v1>"
  }
}
