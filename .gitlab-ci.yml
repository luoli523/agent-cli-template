# Placeholder pipeline. Replaced with the real lint + typecheck + test +
# validate pipeline in T11. See docs/decisions/0002-architecture-reset.md
# for v0.2 reset context, and docs/specs/2026-05-15-di-cli-architecture.md
# for the target architecture.

stages:
  - smoke

default:
  image: alpine:3.20
  interruptible: true

smoke:
  stage: smoke
  script:
    - echo "Placeholder CI for v0.2 scaffold. Real pipeline lands in T11."
    - test -L AGENTS.md
    - readlink AGENTS.md | grep -qx CLAUDE.md
    - test -f docs/specs/2026-05-15-di-cli-architecture.md
    - test -f docs/decisions/0002-architecture-reset.md
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"
